[pytest]
testpaths = tests
# Project root (backend's parent) so `import backend.*` resolves.
pythonpath = ..
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
filterwarnings =
    ignore::DeprecationWarning:transformers.*
    ignore::FutureWarning:torch.*
    ignore::UserWarning:diffusers.*
